# from project root, >>> python2 -m benchmark.sample
import sample

# pysam = sample.PythonSampler(verbose=2)
# pysam.full_sample()

# expr = pysam.get_line_expression(r"""        self.assertTrue(re.match(r"^x{3,4}?$", "xx => ^x{3,4}?$""")
# print "get_line_expression", expr
# print "FAdoize", pysam.FAdoize(expr)